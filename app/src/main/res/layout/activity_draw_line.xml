<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_draw_line"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.cxl.life.app.draw.DrawLineActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/draw_line_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        app:navigationIcon="@mipmap/ic_back"
        app:theme="@style/toolbar_theme">

    </android.support.v7.widget.Toolbar>

    <TextView
        android:id="@+id/draw_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/draw_line_toolbar"
        android:textColor="@color/body_text" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/draw_line_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="@dimen/button_margin"
        app:srcCompat="@android:drawable/ic_menu_directions" />

    <FrameLayout
        android:id="@+id/draw_line_fl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/draw_line_toolbar"
        android:background="@color/translucent"
        android:visibility="gone" />

</RelativeLayout>
